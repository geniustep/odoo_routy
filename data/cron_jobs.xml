<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Cron: Clean Old GPS Logs (Daily) -->
        <record id="cron_clean_old_gps_logs" model="ir.cron">
            <field name="name">Routy: Clean Old GPS Logs</field>
            <field name="model_id" ref="model_routy_gps_log"/>
            <field name="state">code</field>
            <field name="code">model._cron_clean_old_logs()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="priority">20</field>
        </record>

        <!-- Cron: Update Delayed Service Requests (Every 30 minutes) -->
        <record id="cron_check_delayed_requests" model="ir.cron">
            <field name="name">Routy: Check Delayed Service Requests</field>
            <field name="model_id" ref="model_routy_service_request"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_delayed_requests()</field>
            <field name="interval_number">30</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="priority">15</field>
        </record>

        <!-- Cron: Send Driver Reminders (Every 2 hours) -->
        <record id="cron_send_driver_reminders" model="ir.cron">
            <field name="name">Routy: Send Driver Reminders</field>
            <field name="model_id" ref="model_routy_job"/>
            <field name="state">code</field>
            <field name="code">model._cron_send_driver_reminders()</field>
            <field name="interval_number">2</field>
            <field name="interval_type">hours</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="priority">10</field>
        </record>

        <!-- Cron: Daily Summary Report (Daily at 8 PM) -->
        <record id="cron_daily_summary_report" model="ir.cron">
            <field name="name">Routy: Daily Summary Report</field>
            <field name="model_id" ref="model_routy_service_request"/>
            <field name="state">code</field>
            <field name="code">model._cron_send_daily_summary()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="nextcall" eval="(datetime.now() + timedelta(days=1)).replace(hour=20, minute=0, second=0)"/>
            <field name="active" eval="True"/>
            <field name="priority">5</field>
        </record>

        <!-- Cron: Auto-close Old Incidents (Weekly) -->
        <record id="cron_auto_close_incidents" model="ir.cron">
            <field name="name">Routy: Auto-close Old Incidents</field>
            <field name="model_id" ref="model_routy_incident"/>
            <field name="state">code</field>
            <field name="code">model._cron_auto_close_old_incidents()</field>
            <field name="interval_number">7</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="priority">25</field>
        </record>

    </data>
</odoo>
